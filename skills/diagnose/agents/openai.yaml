interface:
  display_name: "Diagnose"
  short_description: "Trace failures through real runtime evidence"
  default_prompt: "Use $diagnose to reproduce this wrong behavior, falsify plausible causes through the real code, runtime, or data path, and fix it only if my request includes implementation."
