interface:
  display_name: "Diagnose"
  short_description: "Find root causes and propose simple, lasting fixes"
  default_prompt: "Use $diagnose to analyze the trajectory data, derive and report the root cause step by step, and propose a simple New Jersey-style repair that fixes the cause rather than its symptoms."
