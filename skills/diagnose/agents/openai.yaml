interface:
  display_name: "Diagnose"
  short_description: "Diagnose failures to verified fixes"
  default_prompt: "Use $diagnose to trace this user-visible failure through the real runtime or data path, fix the root cause minimally, verify the original behavior, and update any stateful source artifact truthfully."
