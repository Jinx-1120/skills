interface:
  display_name: "Diagnose"
  short_description: "Trace failures to root cause and propose simple fixes"
  default_prompt: "Use $diagnose to reconstruct the failure from trajectory evidence, derive and report the root cause step by step, and propose the simplest ownership-correct repair that prevents recurrence."
