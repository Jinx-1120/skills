interface:
  display_name: "Grill Plan"
  short_description: "Converge only material ambiguity"
  default_prompt: "Use $grill-plan to resolve discoverable facts first, ask only consequential blocking decisions, and produce the smallest observable requirements contract for this request."
