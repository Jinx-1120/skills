interface:
  display_name: "Implement"
  short_description: "Implement tasks with outcome verification"
  default_prompt: "Use $implement when this already-defined project task is ready to build, including requested comments, doc comments, or docstrings, with focused verification and truthful updates to any stateful source artifact."
