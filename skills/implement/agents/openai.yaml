interface:
  display_name: "Implement"
  short_description: "Build clear tasks through verified outcomes"
  default_prompt: "Use $implement to carry this clear task through repository inspection, a coherent edit, proportionate verification, read-back or runtime proof where relevant, and a truthful completion report."
