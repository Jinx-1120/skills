interface:
  display_name: "Implement"
  short_description: "Implement tasks with outcome verification"
  default_prompt: "Use $implement to implement this already-defined project task, including requested comments, doc comments, or docstrings, with focused outcome verification and truthful updates to any stateful source artifact."
