interface:
  display_name: "Technical Plan"
  short_description: "Design settled behavior against the real system"
  default_prompt: "Use $technical-plan to ground this mostly settled outcome in the real repository, define ownership and failure contracts at the right risk depth, and produce a buildable verification-backed design."
